<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pearl.mapper.BoardMapper">

	<select id="list" resultType="com.pearl.domain.BoardVO">
	<![CDATA[  
		select * from Board where boardnum > 0
		]]>
	</select>
	
	<select id="read" resultType="com.pearl.domain.BoardVO">
		select * from Board 
		where boardnum = #{boardNum}
	</select>
	
	<insert id="insertNotice">
		insert into Board(boardNum, memNum, boardTitle, boardContent, boardDate, boardType)
		values(BOARD_SEQ.nextval,  21, #{boardTitle}, #{boardContent}, sysdate, 'N')
	</insert>
	
	<delete id="delete">
		delete Board 
		where boardNum=#{boardNum}
	</delete>
	
	<update id="update">
		update Board  
		set bTitle=#{boardTitle}, bContent=#{boardContent}  
		where boardNum = #{boardNum}
	</update>
	
</mapper>
