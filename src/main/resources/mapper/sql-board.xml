<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pearl.mapper.BoardMapper">

	<select id="list" resultType="com.pearl.domain.BoardVO">
	<![CDATA[  
		select * from Board where bnum > 0
		]]>
	</select>
	
	<select id="read" resultType="com.pearl.domain.BoardVO">
		select * from Board 
		where bnum = #{bNum}
	</select>
	
	<insert id="insert">
		insert into Board(bNum, mNum, bTitle, bContent, bDate, bType)
		values(BOARD_SEQ.nextval, #{bNum}, #{mNum}, #{bTitle}, #{bContent},
		sysdate, #{bType})
	</insert>
	
	<delete id="delete">
		delete Board 
		where bNum=#{bNum}
	</delete>
	
	<update id="update">
		update Board  
		set bTitle=#{bTitle}, bContent=#{bContent}  
		where bNum = #{bNum}
	</update>
	
</mapper>
